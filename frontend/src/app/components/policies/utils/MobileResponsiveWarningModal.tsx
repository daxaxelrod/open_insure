import React from "react";

export default function MobileResponsiveWarningModal({
    visible,
    setVisible,
}: {
    visible: boolean;
    setVisible: (visible: boolean) => void;
}) {
    return <div>MobileResponsiveWarningModal</div>;
}
