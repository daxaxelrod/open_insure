import React from "react";

export default function SignifiganceThermometer({
    count,
    requiredCount,
}: {
    count: number;
    requiredCount: number;
}) {
    return <div>SignifiganceThermometer</div>;
}
