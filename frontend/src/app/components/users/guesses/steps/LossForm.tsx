import { Form } from "antd";
import React from "react";

export default function LossForm({ submitForm }: { submitForm: () => void }) {
    const form = Form.useFormInstance();

    return <div>LossForm</div>;
}
